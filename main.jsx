import React from "react";
import ReactDOM from "react-dom/client";
import PlanSalud from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PlanSalud />
  </React.StrictMode>
);
